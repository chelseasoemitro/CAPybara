int greater_than_x(int l, int x) {
    if (l > x) {
        return l;
    }
    else {
        if(l <= x) {
            print_str("check");
        }

        while (l <= x){
            print_int(l);
            l = l + 1;
        }

        print_int(1);
        return l;
    }
    return 0;
}

void main() {
    print_int(greater_than_x(3, 5));

    if (1 > 0){
        print_int(4);
    }
    else {
        int l = 0;
        while (l <= 4){
            print_int(l);
            l = l + 1;
        }
        print_int(l);
    }
}