/* Global variables need to be declared and initialized with literals */
int y = 4 + 5; // expression used - needs literal
int x; // no initialization
int[3][3] mat; // no initialization

void main() {
	x = 5;
 	mat = [1, 2, 3; 4, 5, 6; 7, 8, 9;];
	print(x)
	print_int(mat[2][0]);
}
